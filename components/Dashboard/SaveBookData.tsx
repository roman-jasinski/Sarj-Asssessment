import React from 'react';
import { Button } from '@/components/ui/button';

export default function SaveBookData() {
  return (
    <div>
      <Button className="w-full">Save</Button>
    </div>
  )
}